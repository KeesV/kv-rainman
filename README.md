# Steps to get it compiling

1. `git submodule init`
1. `git submodule update`
1. `platformio lib install "Adafruit GFX Library"`
1. `platformio lib install PubSubClient`
1. `platformio lib install "Adafruit SSD1306"`
